name: build-php
description: Build PHP
author: php

branding:
  icon: package
  color: purple

inputs:
  php-version:
    description: PHP version to build
    required: true
  arch:
    description: Architecture to build
    required: true
  ts:
    description: Thread safety to build
    required: true
  build-branch:
    description: SDK build branch for deps/CRT (set to master to build a pre-GA source against master deps)
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Build PHP
      shell: pwsh
      run: |
        Import-Module ${{ github.action_path }}\BuildPhp -Force
        Invoke-PhpBuild -PhpVersion ${{inputs.php-version}} `
                        -Arch ${{inputs.arch}} `
                        -Ts ${{inputs.ts}} `
                        -BuildBranch '${{inputs.build-branch}}'

    - name: Upload artifacts
      uses: actions/upload-artifact@v5
      with:
        name: artifacts-${{inputs.php-version}}-${{inputs.arch}}-${{inputs.ts}}
        path: artifacts/*
